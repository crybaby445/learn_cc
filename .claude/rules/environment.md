# Environment

This repo uses a devcontainer (Ubuntu 24.04 + Node.js). Claude Code CLI is installed automatically on container start via `.ona/automations.yaml`:

```
npm install -g @anthropic-ai/claude-code
```

No build, test, or lint commands are configured.
