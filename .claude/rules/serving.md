---
paths:
  - "*.html"
  - "**/*.html"
---

# Serving the Site

`index.html` is a self-contained static HTML/CSS file with no bundler or dependencies. To preview locally:

```
npx serve . -l 8080
```

Then open http://localhost:8080.
